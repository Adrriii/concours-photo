version: "3.3"

services:
    front:
        build:
            context: ./front
            dockerfile: ./Dockerfile
        ports: 
            - "8000:80"
        image: front
    
    back:
        build:
            context: ./back
            dockerfile: ./Dockerfile
        ports: 
            - "9000:8080"
        image: back